export class StudentInfo {
  studentId: string;
  name: string;
  address: string;
  constructor(studentId: string, name: string, address: string) {
    this.studentId = studentId;
    this.name = name;
    this.address = address;
  }
}
